#include <stdio.h>
int main() {
    int m,n;
    scanf("%d%d",&m,&n);
    int a[m][n],i,j;
    for(i=0;i<m;i++)
    {
        for(j=0;j<n;j++)
        {
            scanf("%d",&a[i][j]);
        }
    }
    int x=0;
    for(i=0;i<m;i++)
    {
        for(j=0;j<n;j++)
        {
            x=x+a[i][j];
        }
    }
    printf("%d",x);
    return 0;
}
